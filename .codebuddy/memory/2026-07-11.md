# 2026-07-11

- 同步 api-server 到云端：推送 `master`（`bc919ec..baf21e3` security 修复）和新建 `feat/apiserver-security-hardening` 分支（5 提交：500 响应收口、CORS/JWT 修复等），已建 upstream。
- 推送 feat 分支新提交 `84fcca9`（fix: 修正 swagger 注释类型引用以通过 swag init）到云端，`91c6c1b..84fcca9`，已同步。
- 修复 CI 上 swag v1.16.6 `swag init` 报错：`http/controller` 中 12 处 swag 注释用了 import 别名前缀（`request.`/`apiResp.`/`adResp.`）而 swag 按包名解析，改为包名（`api.`/`admin.`）。本地装 swag@v1.16.6 验证两实例 EXIT=0。提交 `5aa9b85` 并推送 feat 分支（7 文件，14 行注释改动，未提交生成的 docs——build.sh 的 CI 自行 swag init）。
- 全部推送：① api-server `feat/apiserver-security-hardening` 推 `5aa9b85..072370a`（含 version 兼容、mfa TOTP 容差）；② client-custom `1.4.8.1` 推 `da0be3f61..dad1c3614`（版本检查轮询、custom 更新）；③ 主仓库 rustdesk-custom `main` 推 `e75af0f..59ce9a4`（子模块引用更新）。三个仓库均与云端一致。
- 复查推送：client-custom 又新增 2 个本地提交（HEAD=`079387a74`），补推 `dad1c3614..079387a74`，`ahead=0`。现三个仓库（api-server feat / client-custom 1.4.8.1 / rustdesk-custom main）均与云端完全一致，主仓库 gitlink 指向的 client-custom `079387a` 已在 origin 存在。
- 修 client-custom CI macOS aarch64 编译错误 `E0382`：common.rs:278 UPnP 续期中 `m`(UpnpMapping 未 Copy) 被 move 进 UPNP_MAPPING 后又借用 `m.external_port`。改为 move 前 copy 出 `external_port`。提交 `dc1593d7c` 推 client-custom `079387a74..dc1593d7c`，主仓库 `59ce9a4..d62c30e` 更新子模块引用。其余 19 warning 不影响编译。
- 再次推送：用户在期间又各加了 2 个提交。api-server 推 `072370a..1f2ee5f`（feat），client-custom 推 `dc1593d7c..22bcce63d`（1.4.8.1），主仓库 `d62c30e..bb82ece` 更新两子模块引用。三个仓库现已全部与云端一致。
